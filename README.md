# Disability Stories

Website for the Disability Stories podcast: disabilitystories.com.au

Real conversations from across the disability community in Australia.

- Static site: Eleventy 3, deployed on Render
- `npm install` then `npm run dev` to work locally, `npm run build` to build to `_site/`
- Project rules: CLAUDE.md. Docs: docs/
